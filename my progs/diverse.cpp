#include <iostream>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int min(int a,b)
{
if (a<=b) 
	{ min=a; }
else min=b;
}
int main() {
	int a,b,c,d,e,rez;
	printf ("Introdceti a: ");
	scanf (&a);
	printf ("Introdceti b: ");
	scanf (&b);
	printf ("Introdceti c: ");
	scanf (&c);
	printf ("Introdceti d: ");
	scanf (&d);
	printf ("Introdceti e: ");
	scanf (&e);
	
	rez=min(a,min(b,(min(c,min(d,e))))))
	printf("Minimul dvs este:%d", rez)
	
	return 0;
}
